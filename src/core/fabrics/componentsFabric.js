const componentsFabric = ({ type, styles, children }) => {
  console.log('====================================');
  console.log(type, styles, children);
  console.log('====================================');

  return <>react component</>;
};

export default componentsFabric;
